# requirements.txt

# This file lists all the Python dependencies required to run your project. It ensures that
# anyone setting up the project can install all necessary libraries with a single command
# (pip install -r requirements.txt).


transformers
datasets
scikit-learn
torch
accelerate
pandas
pyyaml
wandb
python-dotenv
fastapi
uvicorn